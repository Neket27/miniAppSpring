server:
  address: 0.0.0.0
  port: 8083

#Swagger
springdoc:
  swagger-ui:
    path: /ui
    operationsSorter: alpha
    tagsSorter: alpha
  api-docs:
    path: /api-docs

spring:
  application:
    name: payment-service

  security:
    oauth2:
      resource:
      server:
        jwt:
          issuer-uri: http://localhost:9000

  # DB
  datasource:
    url: jdbc:postgresql://localhost:5434/pay-service-miniapp_db
    username: pay-service-miniapp
    password: pay-service-miniapp
    driverClassName: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        hbm2ddl:
          auto: update
        globally_quoted_identifiers: true # ( user, order, group)
    generate-ddl: true
    show-sql: false

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml
    password: pay-service-miniapp
    user: pay-service-miniapp

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/
    register-with-eureka: true
    fetch-registry: true

#Log
logging:
  level:
    root: info



